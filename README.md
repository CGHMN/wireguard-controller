## Wireguard HTTP API/Controller

Starts and configures Wireguard interfaces based on a database, to allow servers to be configured entirely via HTTP.

This might be used for the self-service and automated sign-up front-end for member tunnels.

(Readme will be written out more soon)